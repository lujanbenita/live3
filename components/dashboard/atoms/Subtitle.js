import styled from "@emotion/styled";
import { FONT_MUSEO_SANS, SUBTITLE_COLOR } from "theme";

const Subtitle = styled.h5`
  color: ${SUBTITLE_COLOR};
  font-family: ${FONT_MUSEO_SANS};
  font-size: 10px;
  font-weight: bold;
  line-height: normal;
`;

export default Subtitle;
