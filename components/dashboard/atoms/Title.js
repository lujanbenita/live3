import styled from "@emotion/styled";
import { FONT_MUSEO_SANS, TITLE_COLOR } from "theme";

const Title = styled.h4`
  color: ${TITLE_COLOR};
  font-family: ${FONT_MUSEO_SANS};
  font-size: 13px;
  font-weight: bold;
  line-height: normal;
`;

export default Title;
