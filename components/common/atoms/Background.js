import styled from "@emotion/styled";

const Background = styled.div`
  background: radial-gradient(circle at 50% 0, #2a4f6b, #16202c 76%);
  height: 100vh;
  left: 0;
  position: fixed;
  top: 0;
  width: 100vw;
  z-index: -1;
`;

export default Background;
